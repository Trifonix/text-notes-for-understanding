# Задача 2: Паспорт для функции (Docstrings)
# В Python есть особый вид комментариев — строки документации (docstrings). Они пишутся в тройных кавычках """ сразу под объявлением функции и помогают другим программистам (и IDE) понять, как использовать твой код.
# Твое задание: Напиши правильный docstring для функции ниже. Он должен включать:
# Краткое описание того, что делает функция.
# Описание аргументов (price и discount_percent).
# Описание того, что функция возвращает.

"""
Функция итоговой цены
Параметры: стоимость, скидка в процентах.
Функция возвращает исходную стоимость или цену с учётом скидки.
"""
def get_final_price(price, discount_percent):
    if discount_percent < 0 or discount_percent > 100:
        return price
    discount_amount = price * (discount_percent / 100)
    return price - discount_amount

'''
Ошибка позиционирования
В Python строка документации (docstring) должна находиться внутри самой функции, сразу после строки def, с таким же отступом, как и остальной код.
Если написать её над функцией, то Python воспримет это просто как висящую в воздухе строку, и встроенная система помощи (help()), а также подсказки в редакторах кода (PyCharm, VS Code) её не увидят.
'''

# В мире Python есть стандарты оформления (самый популярный стиль — от Google). Вот как выглядит идеальный docstring для твоей функции с правильным расположением:
def get_final_price_good(price, discount_percent):
    """
    Рассчитывает итоговую цену товара с учетом скидки.

    Аргументы:
        price (float): Исходная стоимость товара.
        discount_percent (float): Размер скидки в процентах (от 0 до 100).

    Возвращает:
        float: Итоговую цену со скидкой. Если процент указан неверно 
               (меньше 0 или больше 100), возвращает исходную стоимость.
    """
    if discount_percent < 0 or discount_percent > 100:
        return price
    discount_amount = price * (discount_percent / 100)
    return price - discount_amount